import React,{useMemo,useState}from"react";
import{PDFDocument,degrees}from"pdf-lib";
import{Archive,CheckCircle2,Clock,Download,Eye,FileQuestion,FileText,FileUp,Globe2,Languages,Layers3,Lock,Mail,MessageCircle,Presentation,Scissors,Search,ShieldCheck,Sparkles,Trash2,Wand2,Zap,BookOpenCheck,SplitSquareHorizontal,X,RotateCw,Eraser}from"lucide-react";

const tools=[
{id:"pdf-translator",title:"PDF Translator",ar:"ترجمة PDF",icon:Languages,status:"Beta",category:"AI",desc:"Beta translation for small text-based PDFs.",arDesc:"ترجمة تجريبية لملفات PDF النصية الصغيرة."},
{id:"word-translator",title:"Word Translator",ar:"ترجمة Word",icon:Languages,status:"Beta",category:"AI",desc:"Translate DOCX documents with smart paragraph handling.",arDesc:"ترجمة ملفات DOCX الصغيرة مع معالجة ذكية للفقرات."},
{id:"pdf-summarizer",title:"PDF Summarizer",ar:"تلخيص PDF",icon:Sparkles,status:"Available",category:"AI",desc:"Get a summary and key points from text-based PDFs.",arDesc:"احصل على ملخص واضح ونقاط مهمة من ملفات PDF النصية."},
{id:"extract-questions",title:"Extract Questions",ar:"استخراج أسئلة",icon:FileQuestion,status:"Beta",category:"AI",desc:"Turn study files into review questions.",arDesc:"حوّل ملفات الدراسة إلى أسئلة مراجعة."},
{id:"study-mode",title:"Student Mode",ar:"وضع الطالب",icon:BookOpenCheck,status:"Beta",category:"AI",desc:"Summary, key terms, questions, and notes.",arDesc:"ملخص، مصطلحات، أسئلة، وملاحظات مراجعة."},
{id:"compare-view",title:"Original vs Output",ar:"مقارنة قبل وبعد",icon:SplitSquareHorizontal,status:"Coming Soon",category:"AI",desc:"Preview original and output side by side.",arDesc:"معاينة المحتوى الأصلي والنتيجة جنبًا إلى جنب."},
{id:"create-ppt",title:"Create PowerPoint",ar:"توليد عرض",icon:Presentation,status:"Coming Soon",category:"AI",desc:"Generate slides from text or PDF.",arDesc:"توليد عرض تقديمي من نص أو PDF."},
{id:"word-to-pdf",title:"Word to PDF",ar:"Word إلى PDF",icon:FileText,status:"Coming Soon",category:"Convert",desc:"Convert DOCX files into PDF documents.",arDesc:"حوّل ملفات DOCX إلى PDF."},
{id:"pdf-to-word",title:"PDF to Word",ar:"PDF إلى Word",icon:FileText,status:"Coming Soon",category:"Convert",desc:"Convert PDFs into editable Word files.",arDesc:"حوّل PDF إلى ملف Word قابل للتعديل."},
{id:"pdf-to-ppt",title:"PDF to PowerPoint",ar:"PDF إلى PowerPoint",icon:Presentation,status:"Coming Soon",category:"Convert",desc:"Convert PDFs into editable slides.",arDesc:"حوّل PDF إلى شرائح قابلة للتعديل."},
{id:"merge-pdf",title:"Merge PDF",ar:"دمج PDF",icon:Archive,status:"Available",category:"PDF",desc:"Combine multiple PDFs in your browser.",arDesc:"ادمج أكثر من ملف PDF مباشرة داخل المتصفح."},
{id:"split-pdf",title:"Extract Pages",ar:"تقسيم PDF",icon:Scissors,status:"Available",category:"PDF",desc:"Extract selected pages into a new PDF.",arDesc:"استخرج صفحات محددة في ملف PDF جديد."},
{id:"remove-pages",title:"Remove Pages",ar:"حذف صفحات",icon:Eraser,status:"Available",category:"PDF",desc:"Remove unwanted pages from a PDF.",arDesc:"احذف صفحات غير مرغوبة من ملف PDF."},
{id:"rotate-pdf",title:"Rotate PDF",ar:"تدوير PDF",icon:RotateCw,status:"Available",category:"PDF",desc:"Rotate all or selected PDF pages.",arDesc:"دوّر كل صفحات PDF أو صفحات محددة."},
{id:"compress-pdf",title:"Compress PDF",ar:"ضغط PDF",icon:Archive,status:"Beta",category:"PDF",desc:"Reduce file size for easier sharing.",arDesc:"قلّل حجم الملف لتسهيل مشاركته."}
];
const categories=["All","AI","Convert","PDF"];
function saveBlob(bytes,name){const blob=new Blob([bytes],{type:"application/pdf"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}
function parsePages(input,total,isRTL){const txt=(input||"").trim();if(!txt)return Array.from({length:total},(_,i)=>i);const out=[];for(const part of txt.split(",").map(x=>x.trim()).filter(Boolean)){if(part.includes("-")){const[a,b]=part.split("-").map(x=>Number(x.trim()));if(!a||!b||a>b)throw new Error(isRTL?"صيغة الصفحات غير صحيحة.":"Invalid page range.");for(let p=a;p<=b;p++)out.push(p)}else{const p=Number(part);if(!p)throw new Error(isRTL?"صيغة الصفحات غير صحيحة.":"Invalid page number.");out.push(p)}}const u=[...new Set(out)];if(u.some(p=>p<1||p>total))throw new Error(isRTL?`أدخل صفحات بين 1 و ${total}.`:`Enter pages between 1 and ${total}.`);return u.map(p=>p-1)}
function InfoModal({type,lang,onClose}){if(!type)return null;const r=lang==="ar";const c={privacy:{icon:ShieldCheck,title:r?"سياسة الخصوصية":"Privacy Policy",body:r?["نستخدم الملفات فقط لتنفيذ الأداة التي تختارها.","أدوات PDF الحالية تعمل داخل المتصفح ولا ترفع الملفات إلى خادم.","لا نستخدم محتوى ملفاتك لتدريب النماذج."]:["We use uploaded files only to perform the selected tool.","The current PDF tools run in your browser without server upload.","We do not use your document content to train models."]},terms:{icon:FileText,title:r?"شروط الاستخدام":"Terms of Use",body:r?["DocLingo AI حاليًا في نسخة تجريبية.","راجع النتائج قبل الاستخدام الرسمي.","ارفع الملفات التي تملك حق استخدامها فقط."]:["DocLingo AI is currently in beta.","Review outputs before official use.","Only upload files you have rights to process."]},contact:{icon:MessageCircle,title:r?"تواصل معنا":"Contact Us",body:r?["للاقتراحات أو الإبلاغ عن مشكلة، استخدم زر الانضمام للنسخة التجريبية.","سنضيف نموذج تواصل كامل لاحقًا."]:["For suggestions or issue reports, use the beta access button.","A full contact form will be added later."]}}[type];const Icon=c.icon;return <div className="modal-backdrop" onClick={onClose}><div className="modal" dir={r?"rtl":"ltr"} onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={onClose}><X size={18}/></button><div className="modal-icon"><Icon size={24}/></div><h2>{c.title}</h2><ul className="info-list">{c.body.map((x,i)=><li key={i}>{x}</li>)}</ul><button className="btn btn-primary full" onClick={onClose}>{r?"موافق":"Got it"}</button></div></div>}
function BetaModal({lang,onClose}){const[email,setEmail]=useState("");const[ok,setOk]=useState(false);const r=lang==="ar";return <div className="modal-backdrop" onClick={onClose}><div className="modal" dir={r?"rtl":"ltr"} onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={onClose}><X size={18}/></button>{ok?<><div className="modal-icon success"><CheckCircle2 size={24}/></div><h2>{r?"تم تسجيل اهتمامك":"You're on the beta list"}</h2><p className="muted">{r?"سنربط النموذج بقاعدة بيانات في المرحلة القادمة.":"This form will connect to a database in the next phase."}</p><button className="btn btn-primary full" onClick={onClose}>{r?"تمام":"Done"}</button></>:<><div className="modal-icon"><Mail size={24}/></div><h2>{r?"انضم للوصول المبكر":"Join early access"}</h2><p className="muted">{r?"سجّل بريدك لتكون من أوائل المستخدمين عند تفعيل أدوات جديدة.":"Enter your email to be among the first users when new tools go live."}</p><input className="modal-input" type="email" placeholder={r?"بريدك الإلكتروني":"Your email"} value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn btn-primary full" onClick={()=>setOk(true)} disabled={!email.includes("@")}>{r?"انضم للقائمة":"Join list"}</button></>}</div></div>}
function MergePdfModal({lang,onClose}){const r=lang==="ar";const[files,setFiles]=useState([]);const[busy,setBusy]=useState(false);const[err,setErr]=useState("");async function run(){setErr("");if(files.length<2)return setErr(r?"اختر ملفين PDF على الأقل.":"Choose at least two PDF files.");try{setBusy(true);const merged=await PDFDocument.create();for(const f of files){const pdf=await PDFDocument.load(await f.arrayBuffer());const pages=await merged.copyPages(pdf,pdf.getPageIndices());pages.forEach(p=>merged.addPage(p))}saveBlob(await merged.save(),"doclingo-merged.pdf")}catch(e){setErr(e.message|| (r?"تعذر دمج الملفات.":"Could not merge files."))}finally{setBusy(false)}}return <div className="modal-backdrop" onClick={onClose}><div className="modal" dir={r?"rtl":"ltr"} onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={onClose}><X size={18}/></button><div className="modal-icon"><Archive size={24}/></div><h2>{r?"دمج PDF":"Merge PDF"}</h2><p className="muted">{r?"اختر ملفين PDF أو أكثر. يمكنك إضافتها مرة واحدة أو ملفًا بعد ملف.":"Choose two or more PDFs. Add them all at once or one by one."}</p><input className="modal-input" type="file" accept="application/pdf,.pdf" multiple onChange={e=>{const fs=Array.from(e.target.files||[]);setFiles(prev=>[...prev,...fs]);e.target.value=""}}/>{files.length>0&&<div className="file-list">{files.map((f,i)=><div key={f.name+i}><CheckCircle2 size={15}/> {f.name}</div>)}</div>}{files.length>0&&<button className="btn btn-secondary full" onClick={()=>{setFiles([]);setErr("")}}>{r?"مسح الملفات المختارة":"Clear selected files"}</button>}{err&&<div className="error-note">{err}</div>}<button className="btn btn-primary full" onClick={run} disabled={busy}>{busy?(r?"جاري الدمج...":"Merging..."):(r?"دمج وتحميل":"Merge and download")}</button></div></div>}
function SinglePdfInput({file,setFile,isRTL}){return <><input className="modal-input" type="file" accept="application/pdf,.pdf" onChange={e=>setFile(e.target.files?.[0]||null)}/>{file&&<div className="file-list"><div><CheckCircle2 size={15}/> {file.name}</div></div>}</>}
function SplitPdfModal({lang,onClose}){const r=lang==="ar";const[file,setFile]=useState(null);const[pages,setPages]=useState("1");const[busy,setBusy]=useState(false);const[err,setErr]=useState("");async function run(){setErr("");if(!file)return setErr(r?"اختر ملف PDF أولًا.":"Choose a PDF file first.");try{setBusy(true);const src=await PDFDocument.load(await file.arrayBuffer());const idx=parsePages(pages,src.getPageCount(),r);const out=await PDFDocument.create();(await out.copyPages(src,idx)).forEach(p=>out.addPage(p));saveBlob(await out.save(),"doclingo-split.pdf")}catch(e){setErr(e.message|| (r?"تعذر تقسيم الملف.":"Could not split file."))}finally{setBusy(false)}}return <ToolShell title={r?"تقسيم PDF":"Split PDF"} icon={Scissors} lang={lang} onClose={onClose} desc={r?"اختر ملف PDF ثم اكتب الصفحات المطلوبة مثل: 1-3 أو 2,5,7.":"Choose a PDF, then enter pages like: 1-3 or 2,5,7."}><SinglePdfInput file={file} setFile={setFile} isRTL={r}/><input className="modal-input" value={pages} onChange={e=>setPages(e.target.value)} placeholder={r?"مثال: 1-3,5":"Example: 1-3,5"}/>{err&&<div className="error-note">{err}</div>}<button className="btn btn-primary full" onClick={run} disabled={busy}>{busy?(r?"جاري التقسيم...":"Splitting..."):(r?"تقسيم وتحميل":"Split and download")}</button></ToolShell>}
function RemovePagesModal({lang,onClose}){const r=lang==="ar";const[file,setFile]=useState(null);const[pages,setPages]=useState("1");const[busy,setBusy]=useState(false);const[err,setErr]=useState("");async function run(){setErr("");if(!file)return setErr(r?"اختر ملف PDF أولًا.":"Choose a PDF file first.");try{setBusy(true);const src=await PDFDocument.load(await file.arrayBuffer());const remove=new Set(parsePages(pages,src.getPageCount(),r));const keep=src.getPageIndices().filter(i=>!remove.has(i));if(!keep.length)throw new Error(r?"لا يمكن حذف كل الصفحات.":"You cannot remove all pages.");const out=await PDFDocument.create();(await out.copyPages(src,keep)).forEach(p=>out.addPage(p));saveBlob(await out.save(),"doclingo-pages-removed.pdf")}catch(e){setErr(e.message|| (r?"تعذر حذف الصفحات.":"Could not remove pages."))}finally{setBusy(false)}}return <ToolShell title={r?"حذف صفحات":"Remove Pages"} icon={Eraser} lang={lang} onClose={onClose} desc={r?"اكتب الصفحات التي تريد حذفها مثل: 2 أو 1-3,5.":"Enter pages to remove, like: 2 or 1-3,5."}><SinglePdfInput file={file} setFile={setFile} isRTL={r}/><input className="modal-input" value={pages} onChange={e=>setPages(e.target.value)} placeholder={r?"صفحات الحذف: 2,5 أو 1-3":"Pages to remove: 2,5 or 1-3"}/>{err&&<div className="error-note">{err}</div>}<button className="btn btn-primary full" onClick={run} disabled={busy}>{busy?(r?"جاري الحذف...":"Removing..."):(r?"حذف وتحميل":"Remove and download")}</button></ToolShell>}
function RotatePdfModal({lang,onClose}){const r=lang==="ar";const[file,setFile]=useState(null);const[pages,setPages]=useState("");const[angle,setAngle]=useState("90");const[busy,setBusy]=useState(false);const[err,setErr]=useState("");async function run(){setErr("");if(!file)return setErr(r?"اختر ملف PDF أولًا.":"Choose a PDF file first.");try{setBusy(true);const pdf=await PDFDocument.load(await file.arrayBuffer());const idx=parsePages(pages,pdf.getPageCount(),r);idx.forEach(i=>pdf.getPage(i).setRotation(degrees(Number(angle))));saveBlob(await pdf.save(),"doclingo-rotated.pdf")}catch(e){setErr(e.message|| (r?"تعذر تدوير الصفحات.":"Could not rotate pages."))}finally{setBusy(false)}}return <ToolShell title={r?"تدوير PDF":"Rotate PDF"} icon={RotateCw} lang={lang} onClose={onClose} desc={r?"اترك خانة الصفحات فارغة لتدوير كل الصفحات، أو اكتب مثل: 1-3,5.":"Leave pages empty to rotate all pages, or enter: 1-3,5."}><SinglePdfInput file={file} setFile={setFile} isRTL={r}/><input className="modal-input" value={pages} onChange={e=>setPages(e.target.value)} placeholder={r?"الصفحات: اتركها فارغة للكل":"Pages: leave empty for all"}/><select className="modal-input" value={angle} onChange={e=>setAngle(e.target.value)}><option value="90">90°</option><option value="180">180°</option><option value="270">270°</option></select>{err&&<div className="error-note">{err}</div>}<button className="btn btn-primary full" onClick={run} disabled={busy}>{busy?(r?"جاري التدوير...":"Rotating..."):(r?"تدوير وتحميل":"Rotate and download")}</button></ToolShell>}
function ToolShell({title,icon:Icon,lang,onClose,desc,children}){const r=lang==="ar";return <div className="modal-backdrop" onClick={onClose}><div className="modal" dir={r?"rtl":"ltr"} onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={onClose}><X size={18}/></button><div className="modal-icon"><Icon size={24}/></div><h2>{title}</h2><p className="muted">{desc}</p>{children}</div></div>}
function ToolModal({selectedTool,lang,onClose}){if(!selectedTool)return null;if(selectedTool.id==="merge-pdf")return <MergePdfModal lang={lang} onClose={onClose}/>;if(selectedTool.id==="split-pdf")return <SplitPdfModal lang={lang} onClose={onClose}/>;if(selectedTool.id==="remove-pages")return <RemovePagesModal lang={lang} onClose={onClose}/>;if(selectedTool.id==="rotate-pdf")return <RotatePdfModal lang={lang} onClose={onClose}/>;const r=lang==="ar";return <ToolShell title={r?selectedTool.ar:selectedTool.title} icon={selectedTool.icon} lang={lang} onClose={onClose} desc={r?"هذه الأداة في النسخة التجريبية وسيتم تفعيلها قريبًا.":"This tool is in beta and will be activated soon."}><div className="result-box"><CheckCircle2 size={18}/><span>{r?"تم تجهيز الواجهة وربط المعالجة هو الخطوة التالية":"The interface is ready; backend processing is next"}</span></div><button className="btn btn-primary full" onClick={onClose}>{r?"تمام":"Got it"}</button></ToolShell>}
function ToolCard({tool,lang,onTry,onJoinBeta}){const Icon=tool.icon;const soon=tool.status==="Coming Soon";return <article className="tool-card fade-in"><div className="tool-header"><div className="tool-icon"><Icon size={20}/></div><span className={`status ${tool.status.toLowerCase().replaceAll(" ","-")}`}>{tool.status}</span></div><div className="tool-body"><h3>{lang==="ar"?tool.ar:tool.title}</h3><p>{lang==="ar"?tool.arDesc:tool.desc}</p></div><div className="tool-actions"><button className="btn btn-primary full" onClick={()=>onTry(tool)}>{soon?(lang==="ar"?"تفاصيل":"Details"):(lang==="ar"?"جرّب الآن":"Try now")}</button>{soon&&<button className="btn btn-secondary" onClick={onJoinBeta}>{lang==="ar"?"نبّهني":"Notify"}</button>}</div></article>}
export default function App(){const[lang,setLang]=useState("en");const[category,setCategory]=useState("All");const[query,setQuery]=useState("");const[uploaded,setUploaded]=useState(null);const[selectedTool,setSelectedTool]=useState(null);const[infoModal,setInfoModal]=useState(null);const[betaOpen,setBetaOpen]=useState(false);const r=lang==="ar";const filtered=useMemo(()=>tools.filter(t=>{const q=query.trim().toLowerCase();return(category==="All"||t.category===category)&&(!q||t.title.toLowerCase().includes(q)||t.ar.includes(q))}),[category,query]);return <div className="app" dir={r?"rtl":"ltr"}><div className="beta-banner">{r?"نسخة تجريبية — يتم تفعيل الأدوات تدريجيًا مع الحفاظ على الخصوصية.":"Beta version — tools are being activated gradually with privacy-first processing."}</div><header className="site-header"><div className="container header-inner"><div className="brand"><div className="brand-mark"><Wand2 size={20}/></div><div><strong>DocLingo AI</strong><small>doclingo-ai.netlify.app</small></div></div><div className="header-actions"><button className="btn btn-secondary" onClick={()=>setLang(lang==="en"?"ar":"en")}><Globe2 size={16}/> {lang==="en"?"العربية":"English"}</button><button className="btn btn-primary hide-mobile" onClick={()=>setBetaOpen(true)}>{r?"انضم للنسخة التجريبية":"Join beta"}</button></div></div></header><main><section className="hero container"><div className="hero-copy slide-up"><span className="pill">{r?"وصول مجاني للنسخة التجريبية للمستخدمين الأوائل":"Free beta access for early users"}</span><h1>{r?"ترجم، حوّل، ولخّص ملفاتك بالذكاء الاصطناعي":"Translate, convert, and summarize documents with AI"}</h1><p>{r?"منصة عالمية لأدوات PDF وWord وPowerPoint. سريعة، بسيطة، وتركّز على الخصوصية بحذف الملفات تلقائيًا بعد المعالجة.":"A global document toolkit for PDF, Word, and PowerPoint. Fast, simple, and privacy-focused with automatic file deletion after processing."}</p><div className="hero-buttons"><a href="#upload" className="btn btn-primary large"><FileUp size={18}/> {r?"ارفع ملفك الآن":"Upload your file"}</a><a href="#tools" className="btn btn-secondary large"><Eye size={18}/> {r?"شاهد الأدوات":"Explore tools"}</a></div><div className="trust-grid"><div><ShieldCheck size={16}/> {r?"حذف تلقائي":"Auto deletion"}</div><div><Lock size={16}/> {r?"تشفير أثناء الرفع":"Encrypted upload"}</div><div><Clock size={16}/> {r?"معالجة محدودة مجانًا":"Limited free processing"}</div></div></div><div className="upload-card slide-up" id="upload"><div className="dropzone"><FileUp size={46}/><h2>{r?"جرّب نموذج الرفع":"Try the upload flow"}</h2><p>{r?"اختر ملف PDF أو Word أو PowerPoint للتجربة.":"Choose a PDF, Word, or PowerPoint file for the beta demo."}</p><input type="file" onChange={e=>setUploaded(e.target.files?.[0]?.name||null)}/>{uploaded&&<div className="file-selected"><CheckCircle2 size={16}/> {uploaded}</div>}</div><div className="quick-actions"><button className="btn btn-secondary" onClick={()=>setSelectedTool(tools.find(t=>t.id==="split-pdf"))}><Scissors size={16}/> {r?"تقسيم PDF":"Split PDF"}</button><button className="btn btn-secondary" onClick={()=>setSelectedTool(tools.find(t=>t.id==="rotate-pdf"))}><RotateCw size={16}/> {r?"تدوير PDF":"Rotate PDF"}</button><button className="btn btn-secondary" onClick={()=>setSelectedTool(tools.find(t=>t.id==="merge-pdf"))}><Download size={16}/> {r?"دمج PDF":"Merge PDF"}</button></div><div className="privacy-note"><div><strong>{r?"خصوصية من البداية":"Private by design"}</strong><p>{r?"أدوات PDF الحالية تعمل داخل المتصفح بدون رفع الملفات.":"Current PDF tools run in the browser without uploading files."}</p></div><Trash2 size={20}/></div></div></section><section className="container tools-section" id="tools"><div className="section-head"><div><h2>{r?"كل أدوات المستندات في مكان واحد":"All document tools in one place"}</h2><p>{r?"متاح، تجريبي، وقادم قريبًا — واجهة مشروع عالمي جاهز للتوسع.":"Available, beta, and coming soon — a global product interface ready to scale."}</p></div><div className="filters"><label className="search-box"><Search size={16}/><input placeholder={r?"ابحث عن أداة":"Search tools"} value={query} onChange={e=>setQuery(e.target.value)}/></label><div className="tabs">{categories.map(c=><button key={c} className={category===c?"active":""} onClick={()=>setCategory(c)}>{c}</button>)}</div></div></div><div className="tools-grid">{filtered.map(t=><ToolCard key={t.id} tool={t} lang={lang} onTry={setSelectedTool} onJoinBeta={()=>setBetaOpen(true)}/>)}</div></section><section className="container features-grid"><div className="feature-card"><ShieldCheck size={32}/><h3>{r?"خصوصية واضحة":"Clear privacy"}</h3><p>{r?"أدوات PDF المتاحة تعمل داخل المتصفح. أدوات الذكاء الاصطناعي ستعمل لاحقًا بحدود واضحة.":"Available PDF tools run in the browser. AI tools will be enabled later with clear limits."}</p></div><div className="feature-card"><Zap size={32}/><h3>{r?"تجربة سريعة":"Fast experience"}</h3><p>{r?"واجهة بسيطة: اختر الأداة، ارفع الملف، ثم حمّل النتيجة.":"Simple flow: choose a tool, upload files, then download the result."}</p></div><div className="feature-card"><Layers3 size={32}/><h3>{r?"جاهز للتوسع":"Ready to scale"}</h3><p>{r?"واجهة إنجليزية وعربية كبداية، مع قابلية إضافة لغات وصفحات SEO لكل أداة.":"English and Arabic first, with room for more languages and SEO pages per tool."}</p></div></section></main><footer className="site-footer"><div className="container footer-inner"><span>© 2026 DocLingo AI. Built for beta launch.</span><nav><button onClick={()=>setInfoModal("privacy")}>Privacy</button><button onClick={()=>setInfoModal("terms")}>Terms</button><button onClick={()=>setInfoModal("contact")}>Contact</button></nav></div></footer><ToolModal selectedTool={selectedTool} lang={lang} onClose={()=>setSelectedTool(null)}/><InfoModal type={infoModal} lang={lang} onClose={()=>setInfoModal(null)}/>{betaOpen&&<BetaModal lang={lang} onClose={()=>setBetaOpen(false)}/>}</div>}
