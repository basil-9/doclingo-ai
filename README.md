# DocLingo AI V18 Real AI Beta

## Setup
Set Netlify environment variables:

```text
OPENAI_API_KEY=your_key
OPENAI_MODEL=gpt-5-mini
```

Then redeploy. PDF editing works without the API key. AI Summary, Questions and Student Mode need the key. Scanned PDFs need OCR later.
